# This project's dev server/tests/build need devDependencies (vite, playwright, eslint).
# The user-level ~/.npmrc sets `omit=["dev"]`, which would skip them on plain `npm install`.
# This overrides it so `npm install` always includes dev dependencies in this project.
include=dev